<% if program.favorited_by?(member) %>
  <%= program.favorites.count %>
  <%= link_to '💖', program_favorite_path(program, Favorite.find_by(program: program)), method: :delete, remote: true %>
<% else %>
   <%= program.favorites.count %>
   <%= link_to '🖤', program_favorite_path(program, Favorite.find_by(program: program)), method: :post, remote: true %>
<% end %>

